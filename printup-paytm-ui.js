(() => {
  'use strict';
  // Legacy Paytm-style home override disabled. The app now uses the shared PRINTUP/UX layer.
  // Kept as a no-op file so the existing server injection remains backward compatible.
})();
