plugins { id 'com.android.application' }

android { namespace 'com.printup.app'; compileSdk 35
    defaultConfig { applicationId 'com.printup.app'; minSdk 23; targetSdk 35; versionCode 1; versionName '1.0.0' }
}

configurations.configureEach {
    resolutionStrategy.eachDependency { details ->
        if (details.requested.group == 'org.jetbrains.kotlin') {
            details.useVersion '1.8.22'
            details.because 'Keep Kotlin stdlib modules on one compatible version for AndroidX build'
        }
    }
}

dependencies { implementation 'androidx.appcompat:appcompat:1.7.0' }
